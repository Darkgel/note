限流的目的：
    1.控制流量
    2.控制用户行为，避免垃圾请求

解决方案
    用一个zset结构记录用户的行为历史，每一个行为都会作为zset中的一个key保存下来
    同一个用户的同一种行为用一个zset记录

    通过score来圈出一个时间窗口。为了节省内存，只需要保留时间窗口内的行为记录，同时如果用户是冷用户，
    滑动时间窗口内的行为是空记录，那么这个zset就可以从内存中移除，不再占用空间

    通过统计滑动窗口内的行为数量与阈值max_count进行比较就可以得出当前行为是否被允许